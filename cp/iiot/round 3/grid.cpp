#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int n{}, m{};

bool recurse(int x, int y, ll r, ll g, ll b) {
    if(x > n || y > m)
        return r == 0 && g == 0 && b == 0;
}

int main() {
    long long r{}, g{}, b{};
    cin >> n >> m >> r >> g >> b;

    cout << "NO" << '\n';

    return 0;
}